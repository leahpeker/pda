---
paths:
  - ".github/**"
---

# GitHub Issue Assignment

When starting work on a GitHub issue, assign yourself immediately so others know it's in progress:

```bash
gh issue edit <N> --add-assignee @me
```

Do this at the start of implementation — after plan approval in `/spec`, or when beginning work in `/quick` / direct mode. Don't block on failure.
