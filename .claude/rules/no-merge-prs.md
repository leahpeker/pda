# Never Merge PRs or Push to Main Branches

- **Never merge a PR** (via `gh pr merge`, git merge + push, or any other method) without explicit user approval in the current conversation.
- **Never push directly to `staging` or `main`** — always use a feature branch and create a PR.
- Creating a PR is fine. Merging it is not — that's the user's call.
